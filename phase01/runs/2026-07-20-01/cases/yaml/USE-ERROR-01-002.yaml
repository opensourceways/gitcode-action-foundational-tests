id: USE-ERROR-01-002
dimensions: [usability]
dimension: usability
priority: P1
title: 字段类型错误信息
intent_ref: INTENT-USE-002
setup:
  repo_fixture: default
  secrets: []
  variables: {}
  branch_protection: default
workflow: |
  on: [push]
  jobs:
    test:
      runs-on: default
      steps:
        - run: echo "valid"
trigger:
  event: push
  as: maintainer
  params: {}
fault_injection: null
assertions:
  - type: nonfunctional
    target: error_message
    eval: llm_assisted
    rubric: "类型错误时报错含字段路径、期望类型、实际类型、行号"
teardown:
  reset: fixture
