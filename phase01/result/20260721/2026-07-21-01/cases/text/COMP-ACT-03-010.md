用例 ID:   COMP-ACT-03-010
维度标签:   [completeness, reliability]
维度:      功能完备性
优先级:    P1
溯源意图:  INTENT-ACT-010
母意图:    —
标题:      build_accelerate 构建加速——基本功能/自定义资源池兼容性/自定义镜像/降级行为

前置条件:
  - 仓库配置了构建加速 workflow
  - 已知自定义资源池兼容性待验证

操作步骤:
  1. 配置 workflow 使用 `uses: official_build_accelerate`，在托管 Runner 上触发
  2. 对比启用与未启用 build_accelerate 的构建时长（需有对比基线）
  3. 在自定义资源池（self-hosted runner）上触发，验证是否正常生效
  4. 在自定义镜像（`container.image`）场景下触发
  5. 模拟加速失败场景（如网络不通），观察是否降级到正常构建还是直接失败
  6. 检查输出信息中是否可观测到加速效果（缓存命中率、加速比）

预期结果:
  - 托管 Runner 上构建时长明显短于未启用
  - 自定义资源池上行为明确（生效或给出不支持的提示）
  - 加速失败时优雅降级到正常构建（而非直接失败）
  - 输出信息中含可观测的加速指标

验证点:
  - [正向] 启用加速后构建时长短于未启用
  - [正向] 自定义资源池行为明确（已知待验证）
  - [正向] 加速失败时降级而非中断
  - [状态] 若无对比基线——标记为 needs-baseline

清理: fixture
