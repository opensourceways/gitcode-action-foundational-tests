用例 ID:   COMP-ACTOR-02-001
维度标签:   [completeness, usability]
维度:      完备性
优先级:    P1
溯源意图:  INTENT-COMP-013
标题:      验证 atomgit.actor 存在性与上下文种类数一致性

前置条件:
  - 工作流可在 push 事件下触发
  - 存在已知的触发用户身份

操作步骤:
  1. 触发工作流运行
  2. 在 job step 中通过表达式读取 atomgit.actor 与 ATOMGIT_ACTOR
  3. 验证值是否为非空字符串且与触发用户一致
  4. 枚举上下文可用性表中实际列出的所有上下文种类，与文档声明的 12 种逐项比对

预期结果:
  - atomgit.actor 存在于 atomgit 上下文且可在 job logs/表达式中读取
  - 值应为触发者用户名/ID，非空且与触发用户一致
  - 上下文总种类数应与文档声明的 12 种一致；若实际为 11 种，需识别缺失种类

验证点:
  - [正向] atomgit.actor 值非空且与触发该次运行的用户一致
  - [正向] 枚举的上下文种类数与 12 种声明逐项比对，记录实际结果
  - [负向] 不应出现「日志示例可用但属性表未声明」导致用户无法确认该属性是否官方支持的情况
  - [非功能] 若种类数确实为 11 而非 12，需明确识别缺失的是哪一种，供文档勘误

清理:      fixture
