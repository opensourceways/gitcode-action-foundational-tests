id: REL-PREEMPT-01-002-V2
dimensions: [reliability]
dimension: reliability
priority: P1
title: preemption events 配置边界值（10 个）应被系统接受并正常工作
intent_ref: INTENT-REL-006

setup:
  repo_fixture: default
  secrets: []
  variables: {}
  branch_protection: default

workflow: |
  on:
    push:
  jobs:
    test:
      name: Preemption boundary accept test
      runs-on: [ubuntu-latest, x64, small]
      steps:
        - name: Checkout source
          uses: checkout
        - name: Echo start
          run: |
            echo "start"

trigger:
  event: push
  as: maintainer
  params: {}

fault_injection: null

assertions:
  - type: positive
    target: run_status
    equals: success
  - type: positive
    target: run_logs
    must_not_contain: preemption error

teardown:
  reset: fixture
