# inputs/business-context/ （建议 · 业务场景）

放 **业务场景、部署方式、历史问题与改造点**。这些是易用性/兼容性/安全 agent 设计贴合实际场景的关键输入——纯读规格造不出「用户真正会踩的坑」。

## 请补充

### 1. Runner 接入方式（必填）
```
# 示例
部署模型:
  - 官方托管 Runner（默认）
  - 自托管主机 Runner（内网环境，需预装特定工具链）
  - 自托管 K8s Runner（弹性伸缩场景）
组织/项目级 Runner 划分策略:
  - <描述>
跨仓库 Runner 复用规则:
  - <描述>
```

### 2. 典型业务工作流模板（建议）
```
# 示例
- 场景：PR 代码检查 → 构建 → 测试 → 部署（多环境）
- 场景：定时安全扫描 + 报告推送
- 场景：多架构 (x64 + arm64) 镜像构建
- 场景：Matrix 多版本 (JDK 8/11/17/21) 测试
```

### 3. 从 GitHub Actions 迁移的重点（建议）
```
# 示例
迁移规模:
  - 预计迁移仓库数: <N>
  - 预计迁移 workflow 数: <N>
迁移改造点:
  - <如：所有 `${{ github.* }}` 上下文全局替换为 `${{ atomgit.* }}`>
  - <如：permissions 字段从 GitHub 命名改为 GitCode 命名>
  - <如：`runs-on` 从 `ubuntu-latest` 改为三段式标签>
已知摩擦:
  - <如：`pull_request` types 命名差异导致 CI 不触发>
  - <如：workflow_dispatch inputs 仅支持 string>
```

### 4. 易用/兼容性测试
```
维度1：文档易用性测试
测试核心：验证GitCode Action官方配套文档对新手、开发者的友好度与实用性。
覆盖场景：官方新手入门文档、核心配置文档（yml语法、触发器、秘钥配置）、官方示例教程、报错问题排查文档、FAQ常见问题、文档检索功能、文档更新时效性、步骤完整性、示例可复用性、新手引导清晰度。
测试重点：文档是否零门槛、步骤是否可直接复刻、是否规避专业盲区、报错解决方案是否有效、检索是否精准、有无无效/缺失内容。
维度2：实操易用性测试
测试核心：验证普通开发者从0到1使用GitCode Action的全流程操作体验、操作成本、交互友好度。
覆盖场景：仓库开启Action功能、Workflow文件创建/编辑/上传、在线编辑器使用、本地yml文件适配、触发器配置（推送/合并/定时）、秘钥与环境变量配置、流水线执行、日志查看、任务重试/终止、报错提示、批量任务管理、新手操作流程、功能入口辨识度。
测试重点：操作步骤繁琐度、交互弹窗友好度、报错提示精准度、配置成本、可视化效果、新手上手难度、异常场景容错性。
维度3：与GitHub Actions 竞品兼容性测试
测试核心：验证GitCode Action对GitHub Actions的语法、文件、插件、运行逻辑的兼容能力，保障跨平台迁移无壁垒。
覆盖场景：GitHub标准Workflow yml文件直接迁移适配、官方语法关键字兼容、常用Action插件/市场组件兼容、各类触发器规则兼容、环境变量/秘钥配置规则兼容、流水线运行逻辑一致性、报错信息对齐度、跨平台迁移无需修改代码的适配能力。
测试重点：正向兼容、反向差异、迁移改造成本、功能等价性、语法容错性。
```

### 格式

Markdown。

## 消费方

- **usability agent**：迁移摩擦 · 典型工作流是否开箱能跑 · 改造点是否文档化
- **compat-diff agent**：迁移改造点列表补足差异盲区（用户告诉你哪里改了，反向验证是否合理）
- **security agent**：部署模型（如自托管 Runner 内网环境）改变攻击面评估
- **reliability agent**：业务工作流模板提供真实负载模式的规模参数
- **orchestrator**：历史问题列表直接喂进风险登记册，调高历史高频风险项的优先级

## 刷新时的影响

更新本目录后，以下命令会触发相关 agent 重新审视：
- `/phase01-update <run-id> dim:usability` → 基于新业务场景重跑易用性 intent
- `/phase01-update <run-id> dim:compat` → 基于新迁移改造点补兼容性 intent
- `/phase01-update <run-id> dim:security` → 基于新部署模型重评估攻击面

> 补充完成后可在此文件末尾记：`已补充 / 日期`。
