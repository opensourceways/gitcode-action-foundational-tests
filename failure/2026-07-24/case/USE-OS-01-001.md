## 失败分诊 · USE-OS-01-001 · runner.os 返回值与文档声明的平台支持一致

**判定结果**: FAIL
**失败断言**: assertions[0] (positive, run_logs) — 期望日志含 `"os=Linux"`（首字母大写），实际日志含 `"os=linux"`（全小写），大小写不匹配

**根因初判**: 产品bug（文档与实现不一致，平台返回与文档声明的大小写格式矛盾）

**证据**:

- **Job 日志全量**（6 行）:
  ```
  === JOB: test runner.os value (status=COMPLETED) ===
  [2026/07/23 22:44:21.055 GMT+08:00] [INFO] Job(1529982528146771968_1529982528121606151) duration check: true
  No shell specified, using platform default: default-bash
  ::debug::Script file created: /home/slave1/runner/workers/0.0.4.4.version/_temp/d1f6d39c-422f-4369-9656-1e6baa3f91ed.sh
  ::debug::Executing: bash -e /home/slave1/runner/workers/0.0.4.4.version/_temp/d1f6d39c-422f-4369-9656-1e6baa3f91ed.sh
  os=linux
  ```
  `${{ runner.os }}` 表达式求值结果为 `linux`（全小写），Job 以 COMPLETED 完成。功能上正确返回了操作系统标识，但值与文档声明的大小写格式不符。

- **预期行为**（Phase 01 文本用例 `USE-OS-01-001`，优先级 P1，维度 usability/compatibility）:
  - 操作步骤 1: "在 workflow 中输出 ${{ runner.os }}"
  - 预期结果: "返回 Linux；若实际仅支持 Linux，文档必须标注 Windows/macOS 待定"
  - 验证点: "[正向] runner.os 返回 Linux"；"[非功能] 文档中 runner.os 说明旁是否附加当前仅 Linux 可用等显式标注"

- **实际行为**:
  - 平台返回 `linux`（全小写），用户收到的是小写的 OS 标识。文档 context.md 声明 `"os": "Linux"`（首字母大写），两者不一致。这是一个文档与实现之间的格式偏差——值存在且正确，但大小写规范不一致可能导致用户在使用 `if: ${{ runner.os == 'Linux' }}` 等条件表达式时出现非预期的比较失败。

- **测试 YAML 与规格精确对照**:
  - 测试 YAML 中 `test-os` job 的步骤:
    ```yaml
    steps:
      - name: echo runner os
        run: |
          echo "os=${{ runner.os }}"
    ```
  - 这对应 GitCode 规格 `phase01/inputs/gitcode-spec/syntax-reference/context.md` 第 211-228 行的 `runner` 上下文定义:
    ```json
    {
      "os": "Linux",
      "arch": "X64",
      "name": "Atomgit Actions 2",
      ...
    }
    ```
    第 213 行示例 JSON 中 `"os": "Linux"` 以及第 223 行表格中 `runner.os` 的说明"操作系统：`Linux`, `Windows`, `macOS`"均使用首字母大写。平台实际返回 `linux`（全小写），与文档声明不一致。

**置信度**: 高（日志第 6 行 `os=linux` 全小写，与 spec context.md 第 213-223 行 `"Linux"` 大写声明直接矛盾，行为偏差确凿）

**建议**:
- 平台需统一 `runner.os` 的返回值格式：(a) 若平台遵循 GitHub Actions 兼容语义（返回 `Linux`），则修复实现使返回值首字母大写；(b) 若平台自行定义，则更新 context.md 第 213 行的示例 JSON 和第 223 行的表格，将所有 OS 名称改为全小写格式并注明与 GitHub Actions 的差异
- 在文档修正前，在 context.md 的 `runner.os` 说明旁附加平台实际返回值格式的注释，避免用户表达式编写时大小写不匹配
- 相关用例: 无直接 sibling（唯一的 runner.os 验证用例）
